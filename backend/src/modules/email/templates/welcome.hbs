<html lang='{{lang}}'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>{{t 'email.welcome.title'}}</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #007bff;
        color: white;
        padding: 20px;
        text-align: center;
      }
      .content {
        padding: 20px;
        border: 1px solid #ddd;
      }
      .button {
        background-color: #007bff;
        color: white;
        padding: 12px 30px;
        text-decoration: none;
        border-radius: 5px;
        display: inline-block;
        margin: 20px 0;
      }
      .footer {
        text-align: center;
        padding: 20px;
        font-size: 12px;
        color: #666;
        border-top: 1px solid #ddd;
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        <h1>{{t 'email.welcome.title'}}</h1>
      </div>
      <div class='content'>
        <p>{{t 'email.welcome.greeting' username=firstName}}</p>
        <p>{{t 'email.welcome.message'}}</p>
        {{#if activationLink}}
          <a href='{{activationLink}}' class='button'>{{t
              'email.welcome.cta'
            }}</a>
        {{/if}}
      </div>
      <div class='footer'>
        <p>{{t 'email.welcome.footer'}}</p>
        <p>©{{currentYear}} {{t 'email.common.copyright'}}</p>
      </div>
    </div>
  </body>
</html>